# FaceTime HD camera (Broadcom 1570, 14e4:1570).
#
# Driven by the out-of-tree facetimehd module, which needs firmware
# extracted from Apple's macOS driver -- see hardware/mba62/install.sh.
# bdc_pci is an old stub that claims the same device.
blacklist bdc_pci
