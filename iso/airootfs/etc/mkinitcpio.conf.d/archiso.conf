# Hooks for the *live* initramfs. The archiso hooks are what let the image
# find and mount the squashfs from the USB stick. Calamares regenerates the
# initramfs for the installed system with the normal hooks.
HOOKS=(base udev microcode modconf archiso block filesystems keyboard)
COMPRESSION="zstd"
