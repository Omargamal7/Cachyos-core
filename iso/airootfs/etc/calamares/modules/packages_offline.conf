# Nothing to install: unpackfs already put the whole system in place. This
# instance exists only so the sequence has a packages module to run, and it
# no-ops without a network.
---
backend: pacman
skip_if_no_internet: true
update_db: false
update_system: false
